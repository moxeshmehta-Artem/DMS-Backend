spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/DMS-DB?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=validate

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# JWT Configuration
dms.app.jwtSecret=${JWT_SECRET:DMSSecretKeyForJWTTokenGenerationWhichShouldBeLongEnoughToMeetRequirements}
dms.app.jwtExpirationMs=86400000

# Logging
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO